<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="es"><generator uri="https://jekyllrb.com/" version="4.4.1">Jekyll</generator><link href="https://darioaplicano.com/feed.xml" rel="self" type="application/atom+xml"/><link href="https://darioaplicano.com/" rel="alternate" type="text/html" hreflang="es"/><updated>2026-09-18T16:16:26+00:00</updated><id>https://darioaplicano.com/feed.xml</id><title type="html">blank</title><subtitle>AIOps Engineer en Telefónica. Más de 10 años de experiencia en datos, Machine Learning, IA Generativa, Big Data y Cloud. Apasionado por convertir problemas complejos en soluciones simples. </subtitle><entry><title type="html">De Chatbots a Agentes: Arquitectura de Sistemas Agénticos en Producción con LangGraph y MCP</title><link href="https://darioaplicano.com/blog/2026/de-chatbots-a-agentes-langgraph-mcp/" rel="alternate" type="text/html" title="De Chatbots a Agentes: Arquitectura de Sistemas Agénticos en Producción con LangGraph y MCP"/><published>2026-09-18T06:00:00+00:00</published><updated>2026-09-18T06:00:00+00:00</updated><id>https://darioaplicano.com/blog/2026/de-chatbots-a-agentes-langgraph-mcp</id><content type="html" xml:base="https://darioaplicano.com/blog/2026/de-chatbots-a-agentes-langgraph-mcp/"><![CDATA[<p>Durante los dos últimos años, la mayor parte de las implementaciones de IA Generativa en empresas se limitaron al paradigma conversacional: una interfaz de chat, una cadena lineal de LangChain o LlamaIndex, y una base de datos vectorial para hacer RAG (Retrieval-Augmented Generation).</p> <p>Aunque este patrón resolvió casos de uso de soporte y consulta documental, <strong>fracasa rotundamente cuando el objetivo es la automatización operativa real</strong>.</p> <p>Un sistema en producción no necesita simplemente responder preguntas sobre documentación; necesita <strong>diagnosticar incidencias, consultar telemetría, correlacionar eventos, proponer acciones y ejecutarlas bajo supervisión</strong>. Para dar ese salto, debemos movernos del modelo <em>«pregunta → respuesta»</em> a sistemas agénticos gobernados por <strong>grafos de estado</strong> y apoyados en protocolos universales de herramientas como <strong>Model Context Protocol (MCP)</strong>.</p> <hr/> <h2 id="1-la-limitación-de-las-cadenas-lineales">1. La limitación de las cadenas lineales</h2> <p>En un flujo tradicional (como un <code class="language-plaintext highlighter-rouge">LLMChain</code> o un pipeline secuencial de LangChain), la ejecución es determinista y unidireccional:</p> \[Input \longrightarrow Prompt \longrightarrow LLM \longrightarrow Output\] <p>En el mundo real de las operaciones y la ingeniería de datos:</p> <ul> <li>Una consulta a la base de datos puede fallar por un timeout o una sintaxis incorrecta.</li> <li>Una métrica anómala requiere profundizar con una segunda consulta a otra fuente antes de concluir un diagnóstico.</li> <li>La salida del modelo puede requerir una validación intermedia o una confirmación humana (<em>Human-in-the-Loop</em>) antes de aplicar un cambio de configuración.</li> </ul> <p>Forzar esta dinámica sobre cadenas lineales produce código frágil, lleno de <code class="language-plaintext highlighter-rouge">try/except</code> anidados y bucles <code class="language-plaintext highlighter-rouge">while</code> difíciles de depurar. Aquí es donde <strong>LangGraph</strong> cambia las reglas del juego.</p> <hr/> <h2 id="2-orquestación-mediante-grafos-de-estado-langgraph">2. Orquestación mediante Grafos de Estado (LangGraph)</h2> <p>LangGraph no concibe al agente como una caja negra que improvisa llamadas en un bucle ciego, sino como una <strong>máquina de estados finita modelada como un grafo dirigido</strong>.</p> <p>Los tres conceptos fundamentales de esta arquitectura son:</p> <ol> <li><strong>Estado Tipado (<code class="language-plaintext highlighter-rouge">State</code>):</strong> Un esquema centralizado (normalmente un <code class="language-plaintext highlighter-rouge">TypedDict</code> o un modelo Pydantic) que viaja a través de todos los nodos. Todo lo que el agente aprende, genera o recibe se acumula en este estado.</li> <li><strong>Nodos (<code class="language-plaintext highlighter-rouge">Nodes</code>):</strong> Funciones o llamadas a modelos que reciben el estado actual, realizan una operación (invocar al LLM, ejecutar una consulta) y retornan una mutación sobre el estado.</li> <li><strong>Aristas Condicionales (<code class="language-plaintext highlighter-rouge">Conditional Edges</code>):</strong> Funciones de decisión que analizan el estado resultante y determinan cuál es el siguiente nodo: ¿debe ejecutarse otra herramienta, debe pedirse validación a un humano o se puede dar por finalizada la tarea?</li> </ol> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">typing</span> <span class="kn">import</span> <span class="n">Annotated</span><span class="p">,</span> <span class="n">TypedDict</span>
<span class="kn">from</span> <span class="n">langgraph.graph</span> <span class="kn">import</span> <span class="n">StateGraph</span><span class="p">,</span> <span class="n">END</span>
<span class="kn">from</span> <span class="n">langgraph.graph.message</span> <span class="kn">import</span> <span class="n">add_messages</span>

<span class="k">class</span> <span class="nc">AgentState</span><span class="p">(</span><span class="n">TypedDict</span><span class="p">):</span>
    <span class="c1"># La anotación add_messages asegura que los mensajes se agreguen cronológicamente
</span>    <span class="n">messages</span><span class="p">:</span> <span class="n">Annotated</span><span class="p">[</span><span class="nb">list</span><span class="p">,</span> <span class="n">add_messages</span><span class="p">]</span>
    <span class="n">sender</span><span class="p">:</span> <span class="nb">str</span>
    <span class="n">requires_human_approval</span><span class="p">:</span> <span class="nb">bool</span>
</code></pre></div></div> <p>Al desacoplar el estado de la ejecución, ganamos dos propiedades críticas para producción:</p> <ul> <li><strong>Persistencia y checkpointing:</strong> Si el contenedor se cae o se requiere aprobación humana durante horas, el estado completo del grafo se persiste (en PostgreSQL, SQLite o Redis) y se reanuda exactamente donde se quedó.</li> <li><strong>Tolerancia a ciclos y límites:</strong> Podemos fijar un umbral estricto de iteraciones (<em>recursion limit</em>) para evitar que un agente entre en un bucle infinito de herramientas consumiendo miles de tokens.</li> </ul> <hr/> <h2 id="3-el-eslabón-que-faltaba-model-context-protocol-mcp">3. El eslabón que faltaba: Model Context Protocol (MCP)</h2> <p>Durante mucho tiempo, conectar un agente a herramientas externas (bases de datos, logs, plataformas cloud) significaba escribir conectores ad-hoc para cada biblioteca o framework. Si cambiabas de LangChain a AutoGen o a un script nativo, tenías que rehacer la integración de herramientas.</p> <p>El <strong>Model Context Protocol (MCP)</strong>, impulsado por Anthropic como estándar abierto, resuelve esto de raíz al establecer un protocolo cliente-servidor universal basado en JSON-RPC:</p> <div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>[ Agente / LangGraph ] 
         │  (JSON-RPC sobre stdio / SSE)
         ▼
[ Servidor MCP ] ────► [ ClickHouse / Azure / APIs / Kubernetes ]
</code></pre></div></div> <h3 id="por-qué-mcp-es-transformador-para-la-ingeniería-de-operaciones">¿Por qué MCP es transformador para la ingeniería de operaciones?</h3> <ol> <li><strong>Desacoplamiento total:</strong> El servidor MCP se ejecuta en su propio entorno (o contenedor aislado), con sus dependencias, credenciales y validaciones de seguridad. El agente no necesita librerías cliente directas.</li> <li><strong>Seguridad y principio de mínimo privilegio:</strong> El servidor MCP expone exclusivamente contratos de herramientas tipados (<code class="language-plaintext highlighter-rouge">tools</code>), recursos de solo lectura (<code class="language-plaintext highlighter-rouge">resources</code>) o plantillas de interacción (<code class="language-plaintext highlighter-rouge">prompts</code>).</li> <li><strong>Reutilización transversal:</strong> El mismo servidor MCP que utilizas para que tu agente LangGraph consulte métricas operativas puede ser consumido por un desarrollador en su IDE o por un CLI automatizado.</li> </ol> <hr/> <h2 id="4-ensamblando-el-patrón-de-la-teoría-al-código">4. Ensamblando el patrón: De la teoría al código</h2> <p>Imaginemos un agente de diagnóstico operativo cuyo objetivo es evaluar la degradación de un servicio de streaming.</p> <h3 id="paso-a-conexión-con-herramientas-mcp">Paso A: Conexión con herramientas MCP</h3> <p>El agente carga el catálogo de herramientas directamente desde el servidor MCP:</p> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="kn">from</span> <span class="n">langchain_core.tools</span> <span class="kn">import</span> <span class="n">tool</span>

<span class="c1"># Supongamos que el servidor MCP expone la herramienta 'query_service_metrics'
# El cliente MCP traduce la definición JSON Schema a una herramienta ejecutable
</span><span class="n">tools</span> <span class="o">=</span> <span class="p">[</span><span class="n">mcp_client</span><span class="p">.</span><span class="nf">get_tool</span><span class="p">(</span><span class="sh">"</span><span class="s">query_service_metrics</span><span class="sh">"</span><span class="p">)]</span>
<span class="n">model</span> <span class="o">=</span> <span class="nc">ChatOpenAI</span><span class="p">(</span><span class="n">model</span><span class="o">=</span><span class="sh">"</span><span class="s">gpt-4o</span><span class="sh">"</span><span class="p">,</span> <span class="n">temperature</span><span class="o">=</span><span class="mi">0</span><span class="p">).</span><span class="nf">bind_tools</span><span class="p">(</span><span class="n">tools</span><span class="p">)</span>
</code></pre></div></div> <h3 id="paso-b-construcción-del-grafo-de-razonamiento-y-acción">Paso B: Construcción del grafo de razonamiento y acción</h3> <div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">workflow</span> <span class="o">=</span> <span class="nc">StateGraph</span><span class="p">(</span><span class="n">AgentState</span><span class="p">)</span>

<span class="c1"># 1. Nodo del modelo: Razona sobre el estado actual
</span><span class="k">def</span> <span class="nf">agent_node</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">AgentState</span><span class="p">):</span>
    <span class="n">response</span> <span class="o">=</span> <span class="n">model</span><span class="p">.</span><span class="nf">invoke</span><span class="p">(</span><span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">])</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="p">[</span><span class="n">response</span><span class="p">],</span> <span class="sh">"</span><span class="s">sender</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">}</span>

<span class="c1"># 2. Nodo de ejecución: Ejecuta las herramientas solicitadas vía MCP
</span><span class="k">def</span> <span class="nf">tool_node</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">AgentState</span><span class="p">):</span>
    <span class="n">last_message</span> <span class="o">=</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">][</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
    <span class="n">results</span> <span class="o">=</span> <span class="p">[]</span>
    <span class="k">for</span> <span class="n">call</span> <span class="ow">in</span> <span class="n">last_message</span><span class="p">.</span><span class="n">tool_calls</span><span class="p">:</span>
        <span class="n">tool_result</span> <span class="o">=</span> <span class="nf">execute_mcp_tool</span><span class="p">(</span><span class="n">call</span><span class="p">[</span><span class="sh">"</span><span class="s">name</span><span class="sh">"</span><span class="p">],</span> <span class="n">call</span><span class="p">[</span><span class="sh">"</span><span class="s">args</span><span class="sh">"</span><span class="p">])</span>
        <span class="n">results</span><span class="p">.</span><span class="nf">append</span><span class="p">(</span><span class="n">tool_result</span><span class="p">)</span>
    <span class="k">return</span> <span class="p">{</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">:</span> <span class="n">results</span><span class="p">,</span> <span class="sh">"</span><span class="s">sender</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">tool</span><span class="sh">"</span><span class="p">}</span>

<span class="c1"># 3. Arista condicional: ¿El modelo solicitó herramientas o respondió?
</span><span class="k">def</span> <span class="nf">should_continue</span><span class="p">(</span><span class="n">state</span><span class="p">:</span> <span class="n">AgentState</span><span class="p">):</span>
    <span class="n">last_message</span> <span class="o">=</span> <span class="n">state</span><span class="p">[</span><span class="sh">"</span><span class="s">messages</span><span class="sh">"</span><span class="p">][</span><span class="o">-</span><span class="mi">1</span><span class="p">]</span>
    <span class="k">if</span> <span class="n">last_message</span><span class="p">.</span><span class="n">tool_calls</span><span class="p">:</span>
        <span class="k">return</span> <span class="sh">"</span><span class="s">tools</span><span class="sh">"</span>
    <span class="k">return</span> <span class="n">END</span>

<span class="c1"># Definición de la topología
</span><span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">,</span> <span class="n">agent_node</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_node</span><span class="p">(</span><span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span> <span class="n">tool_node</span><span class="p">)</span>

<span class="n">workflow</span><span class="p">.</span><span class="nf">set_entry_point</span><span class="p">(</span><span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">)</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_conditional_edges</span><span class="p">(</span><span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">,</span> <span class="n">should_continue</span><span class="p">,</span> <span class="p">{</span>
    <span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">:</span> <span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span>
    <span class="n">END</span><span class="p">:</span> <span class="n">END</span>
<span class="p">})</span>
<span class="n">workflow</span><span class="p">.</span><span class="nf">add_edge</span><span class="p">(</span><span class="sh">"</span><span class="s">tools</span><span class="sh">"</span><span class="p">,</span> <span class="sh">"</span><span class="s">agent</span><span class="sh">"</span><span class="p">)</span>  <span class="c1"># El resultado vuelve al agente para interpretación
</span>
<span class="n">app</span> <span class="o">=</span> <span class="n">workflow</span><span class="p">.</span><span class="nf">compile</span><span class="p">()</span>
</code></pre></div></div> <p>Este flujo permite que el agente consulte una métrica en el servidor MCP, interprete el resultado, decida si necesita correlacionarla con una segunda fuente de datos y solo cuando tenga el diagnóstico completo emita la respuesta final.</p> <hr/> <h2 id="5-lecciones-aprendidas-para-producción">5. Lecciones aprendidas para producción</h2> <p>Diseñar agentes para demos es sencillo; hacer que operen con fiabilidad en sistemas empresariales exige rigor arquitectónico:</p> <h3 id="a-idempotencia-y-acciones-destructivas">A. Idempotencia y acciones destructivas</h3> <p>Las herramientas que leen datos (selects en ClickHouse, inspección de pods, lectura de logs) pueden ejecutarse con relativa libertad. Las herramientas que alteran estado (reiniciar un contenedor, modificar una regla de firewall) <strong>deben exigir confirmación humana explícita</strong> mediante interrupciones en el grafo (<code class="language-plaintext highlighter-rouge">interrupt_before</code>).</p> <h3 id="b-control-de-contexto-y-costes-de-tokens">B. Control de contexto y costes de tokens</h3> <p>Un agente que realiza 6 llamadas sucesivas a herramientas acumula miles de tokens en su historial de mensajes. En producción es indispensable implementar <strong>resúmenes intermedios de memoria</strong> o podar los payloads JSON muy extensos devueltos por las APIs antes de reinyectarlos en el contexto del modelo.</p> <h3 id="c-observabilidad-del-proceso-de-razonamiento">C. Observabilidad del proceso de razonamiento</h3> <p>Monitorear un agente no es medir cuántas llamadas HTTP completó. Requiere registrar:</p> <ul> <li>Cuántos pasos necesitó para resolver la petición.</li> <li>Qué herramientas invocó y con qué argumentos.</li> <li>Cuántos tokens consumió por cada fase (razonamiento vs. ejecución).</li> <li>La tasa de éxito al primer intento vs. reintentos tras errores.</li> </ul> <hr/> <h2 id="conclusión">Conclusión</h2> <p>El valor real de la Inteligencia Artificial en la ingeniería de sistemas no proviene de generar texto elocuente, sino de su capacidad para actuar como un <strong>orquestador cognitivo robusto</strong>.</p> <p>Combinando <strong>LangGraph</strong> para gobernar el flujo de ejecución mediante grafos de estado tipados y <strong>Model Context Protocol (MCP)</strong> para desacoplar y estandarizar la interacción con nuestras plataformas, convertimos a los agentes en piezas de ingeniería predecibles, auditables y listas para operar en entornos reales de misión crítica.</p>]]></content><author><name></name></author><category term="inteligencia-artificial"/><category term="arquitectura"/><category term="agentes-ia"/><category term="langgraph"/><category term="mcp"/><category term="python"/><category term="aiops"/><summary type="html"><![CDATA[Cómo evolucionar de simples flujos lineales de pregunta-respuesta a arquitecturas agénticas basadas en grafos de estado con LangGraph y herramientas desacopladas mediante el Model Context Protocol (MCP).]]></summary></entry></feed>