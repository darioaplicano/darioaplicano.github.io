# GitHub user shown at the top of /repositories/.
# The repo list below is no longer used: _pages/repositories.md ahora carga
# todos los repos públicos (no-fork, no-archived) de este usuario vía la
# GitHub API en cliente. Subes un repo nuevo a GitHub -> al refrescar la
# página aparece. Edita aquí solo si quieres cambiar el usuario mostrado.
github_users:
  - darioaplicano
